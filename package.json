{
  "name": "hiresense",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test:e2e:bootstrap": "pnpm --filter @hiresense/db bootstrap:e2e",
    "test:e2e": "pnpm test:e2e:bootstrap && playwright test",
    "test:e2e:headed": "pnpm test:e2e:bootstrap && playwright test --headed",
    "db:migrate": "pnpm --filter @hiresense/db migrate",
    "db:generate": "pnpm --filter @hiresense/db generate",
    "db:seed": "pnpm --filter @hiresense/db seed",
    "db:studio": "pnpm --filter @hiresense/db studio",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "turbo": "^2.4.0"
  },
  "packageManager": "pnpm@10.12.4",
  "engines": {
    "node": ">=20.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "prisma",
      "@swc/core",
      "esbuild"
    ]
  }
}
